# Entrada de datos
num = float(input("Introduce un número: "))

# El 'rombo' (if) comprueba si es menor que 0
if num < 0:
    print("El número es negativo.")
else:
    # Si no es menor que 0 (es 0 o mayor), es positivo
    print("El número es positivo.")
